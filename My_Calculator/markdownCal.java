//Input  a string in markdown formula and calculate the result
public int matrix_dot(int[][] a, int[][] b){
  if(a.length==0 || a[0].length==0 || b.length==0 || b[0].length==0 || a[0].length!=b.length){
    return -1;
  }
  
  
  
}
